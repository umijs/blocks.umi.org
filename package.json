{
  "scripts": {
    "start": "DEBUG=umi* node src/index.js",
    "deploy": "now && now alias"
  },
  "dependencies": {
    "chalk": "^2.4.1",
    "debug": "^4.1.0",
    "express": "^4.16.4",
    "github-api": "^3.0.0"
  },
  "devDependencies": {
    "now": "^13.1.3"
  }
}
